/**
 * Hosts contain n number of modules.
 */
export class Host extends wsinclude( "base/HostBase" ){
  /**
   * Configures how the host should operate and what host names are allow in what environments.
   */
  SetupHost( ) {

    //<!OPTIONS!>////Host setup goes here//
    
  }
}
