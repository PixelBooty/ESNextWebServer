/**
 * Services contain n number of hosts, databases, and libraries.
 */
export class Service extends wsinclude( "base/ServiceBase" ){
  /**
   * Used to setup your service, connecting databases, and initalizing custom libraries.
   */
  async _SetupService( ) {

    //<!OPTIONS!>////Service setup code//

  }
}
